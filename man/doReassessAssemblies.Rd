% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amplicon_reassess.R
\name{doReassessAssemblies}
\alias{doReassessAssemblies}
\title{Reassess assembled amplicon consensus and unassigned reads}
\usage{
doReassessAssemblies(
  amplicon_out,
  gene_assign = NULL,
  out_dir,
  backend = "edlib",
  consensus_merge_max_edit = 12L,
  read_assign_max_edit = 12L,
  min_identity = NULL,
  layers = c("U1"),
  primer_list = NULL,
  max_primer_edit = 5L,
  skip_missing_backend = TRUE,
  overwrite = FALSE,
  n_core = 1L
)
}
\arguments{
\item{amplicon_out}{Result of \code{\link[=doAssembleAmplicons]{doAssembleAmplicons()}} or path to \verb{amplicon/}.}

\item{gene_assign}{Gene assignments with \code{oriented_seq} (same contract as
assemble). Used to recover U1 sequences when
\code{unassigned_to_cluster.tsv} is present, and as fallback when that file is
missing.}

\item{out_dir}{Output directory (e.g. \code{.../amplicon_reassess}).}

\item{backend}{Character vector of backends to run. Allowed:
\code{"edlib"}, \code{"vsearch"}, \code{"blastn"}, \code{"mmseqs"}. Default \code{"edlib"} provides
an independent exact edit-distance evaluation of clustering results (a
different perspective from vsearch \%identity used in clustering). Multiple
values produce parallel result trees and a \code{summary_compare.tsv}.}

\item{consensus_merge_max_edit}{Max edit distance (bp) used only as a
\strong{highlight threshold} for Q1 pairwise / near-duplicate grouping
(\code{pass_threshold}). No sequences are merged. Identity-based backends use
the derived \code{min_identity} unless that argument is set explicitly.}

\item{read_assign_max_edit}{Max edit distance (bp) for U1 → consensus
reassignment diagnostics (Q2). Same identity derivation as above.}

\item{min_identity}{Optional identity threshold in \eqn{[0,1]} for
\code{vsearch} / \code{blastn} / \code{mmseqs}. If \code{NULL}, derived from edit distance and
median consensus length via \code{\link[=.vsearch_id_from_edit]{.vsearch_id_from_edit()}}.}

\item{layers}{Unassigned layers to include. Currently \code{"U1"} only.}

\item{primer_list}{Optional primers for insert trim when reconstructing U1
sequences from \code{gene_assign}.}

\item{max_primer_edit}{Primer trim edit distance.}

\item{skip_missing_backend}{If \code{TRUE} (default), backends whose binary is
missing on \code{PATH} are skipped with a warning. If \code{FALSE}, stop.}

\item{overwrite}{If \code{FALSE}, stop when \code{out_dir} is non-empty.}

\item{n_core}{Reserved / passed to primer trim.}
}
\value{
A list with \code{out_dir}, \code{summary}, \code{summary_compare}, \code{backends_run},
and \code{samples}.
}
\description{
Diagnostic step after \code{\link[=doAssembleAmplicons]{doAssembleAmplicons()}}. Computes (Q1) pairwise
consensus similarity (and optional near-duplicate groups for review) and
(Q2) whether U1 unassigned reads can be reassigned to an existing
consensus. Does \strong{not} auto-merge or rewrite sequences: the user chooses
which consensus to keep from the pairwise report.
}
\details{
Does \strong{not} modify \verb{amplicon/}. Results are written under \code{out_dir}
(typically \verb{amplicon_reassess/}), with one subdirectory per \code{backend}
so edlib / VSEARCH / BLASTN / MMseqs2 can be compared.
}
