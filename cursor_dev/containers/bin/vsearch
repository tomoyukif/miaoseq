#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
SIF="${MIAOSEQ_VSEARCH_SIF:-$ROOT/sif/vsearch.sif}"
if [[ ! -f "$SIF" ]]; then
  echo "vsearch SIF missing: $SIF (run pull_sifs.sh)" >&2
  exit 127
fi
exec apptainer exec --bind "/home/$USER,/tmp" "$SIF" vsearch "$@"
